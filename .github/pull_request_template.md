## Ticket

DT-<id> (the PR title must start with `DT-<id>:`)

## What changed

-

## How I tested it

- [ ] Hub tests (`pytest`)
- [ ] Dashboard build (`npm run build`)
- [ ] Android build (`android\gradlew.bat -p android assembleDebug` on Windows, `./android/gradlew -p android assembleDebug` on macOS/Linux)
- [ ] Tried it on a real device:

## Screenshots (UI changes)

## Checklist

- [ ] Only touched the files listed in the ticket (the layout is fixed by DT-1)
- [ ] No secrets, keys, databases or personal activity data (exported Shortcuts use Import Questions)
- [ ] Bug review done: findings posted, and each one fixed or answered on the PR (tick before merging)
