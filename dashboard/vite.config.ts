// DT-1 skeleton. DT-30 adds vite-plugin-pwa and the dev proxy to the hub.
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [react()],
});
