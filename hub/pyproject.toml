[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "daytrace-hub"
dynamic = ["version"]
description = "Daytrace hub: local-first, cross-device activity hub with a local AI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "zeroconf>=0.132",
    "qrcode[pil]>=7.4",
    "httpx>=0.27",
    "pandas>=2.2",
    "scipy>=1.13",
    "openai>=1.40",
    "psutil>=5.9",
    "pywin32>=306; sys_platform == 'win32'",
    "winotify>=1.1; sys_platform == 'win32'",
    "pyobjc-framework-Cocoa>=10.3; sys_platform == 'darwin'",
    "pyobjc-framework-Quartz>=10.3; sys_platform == 'darwin'",
]

[project.optional-dependencies]
# ruff is pinned exactly: CI reads this pin, so a new ruff release can't break PRs by itself.
dev = ["pytest>=8", "ruff==0.16.9", "jsonschema>=4.22"]

[project.scripts]
daytrace-hub = "daytrace_hub.__main__:main"

[tool.setuptools.dynamic]
version = { attr = "daytrace_hub.__version__" }

[tool.setuptools.packages.find]
include = ["daytrace_hub*"]

[tool.setuptools.package-data]
daytrace_hub = ["migrations/*.sql", "data/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
