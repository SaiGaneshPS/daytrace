# Browser extension (DT-18)

Sends only the active tab's **domain** (never the full URL) to your local hub.
Load it unpacked: Edge or Chrome > Extensions > Developer mode > Load unpacked > this folder.
